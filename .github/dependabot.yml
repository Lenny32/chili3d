version: 2
updates:
    # Keeps the SHA-pinned actions in .github/workflows up to date.
    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
      # Wait a week before proposing a new release, so compromised or broken versions get caught first.
      cooldown:
          default-days: 7
      groups:
          actions:
              patterns: ["*"]
